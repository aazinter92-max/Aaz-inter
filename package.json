{
  "name": "aaz-international",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && mkdir -p dist && cp -rn frontend/dist/* dist/"
  }
}
