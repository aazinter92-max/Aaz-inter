╔══════════════════════════════════════════════════════════════╗
║  🚨 EMAIL VERIFICATION NOT WORKING - IMMEDIATE FIX NEEDED    ║
╚══════════════════════════════════════════════════════════════╝

PROBLEM: Gmail App Password is INVALID or EXPIRED

═══════════════════════════════════════════════════════════════

✅ SOLUTION (Takes 2 minutes):

STEP 1: Generate NEW Gmail App Password
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Open: https://myaccount.google.com/apppasswords
2. Sign in to: aazinter92@gmail.com
3. If you see "2-Step Verification is not turned on":
   - Go to: https://myaccount.google.com/security
   - Enable "2-Step Verification" first
   - Then come back to app passwords
4. Click "Select app" → Choose "Mail"
5. Click "Select device" → Choose "Other (Custom name)"
6. Type: "AAZ Backend"
7. Click "Generate"
8. Copy the 16-character password (example: abcd efgh ijkl mnop)

STEP 2: Update .env File
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Open: backend/.env

Find this line:
SMTP_PASSWORD=jsestjunyetszkiv

Replace with your NEW password (REMOVE ALL SPACES):
SMTP_PASSWORD=abcdefghijklmnop

Example:
If Google shows: "abcd efgh ijkl mnop"
You type: "abcdefghijklmnop" (no spaces!)

STEP 3: Test Email
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
cd backend
node test-email.js

✅ Success = "Message sent successfully"
❌ Failed = Generate new password again

STEP 4: Restart Backend
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
cd backend
npm start

═══════════════════════════════════════════════════════════════

🔍 WHY THIS HAPPENED:

Current password "jses tjun yets zkiv" is either:
- Expired
- Never generated correctly
- Has wrong format

═══════════════════════════════════════════════════════════════

⚡ ALTERNATIVE: Use SendGrid (If Gmail doesn't work)

1. Sign up FREE: https://signup.sendgrid.com/
2. Verify email
3. Create API Key: Settings → API Keys → Create API Key
4. Update .env:

SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_EMAIL=apikey
SMTP_PASSWORD=SG.your_actual_api_key_here

═══════════════════════════════════════════════════════════════

📝 CURRENT STATUS:

Your app is working BUT emails are NOT being sent.
Users can still register (auto-verified) but won't receive OTP.

After fixing, users will receive:
✅ Registration verification codes
✅ Password reset emails
✅ Order confirmations

═══════════════════════════════════════════════════════════════
